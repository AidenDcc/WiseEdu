# 生产/联调环境示例：复制为 .env.production 并按需调整
# 全部走真实后端
VITE_USE_MOCK=false
VITE_API_BASE_URL=https://api.example.com
# 如仍有个别服务未就绪，可单独保留 Mock：VITE_USE_MOCK=false 时本配置不生效；
# 混合模式示例：全局 Mock，仅 auth 已接通后端
# VITE_USE_MOCK=true
# VITE_REMOTE_SERVICES=auth
