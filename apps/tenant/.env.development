# ===== Mock / 后端切换配置（详见根目录 README） =====
# 全局 Mock 开关：true=全部走 Mock；false=全部走真实后端
VITE_USE_MOCK=true
# 真实后端网关地址（配合 vite.config.ts 的 proxy 使用）
VITE_API_BASE_URL=/api
# 按服务前缀强制走真实后端（逗号分隔），用于后端逐服务灰度切换，如：auth,tenant
VITE_REMOTE_SERVICES=
# 平台演示总入口地址（登录页「返回入口」跳转用）
VITE_PORTAL_URL=http://localhost:5172
