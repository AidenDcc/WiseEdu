# ===== Mock / 后端切换配置（详见根目录 README） =====
# 全局 Mock 开关：true=全部走 Mock；false=全部走真实后端
VITE_USE_MOCK=true
# 真实后端网关地址（配合 vite.config.ts 的 proxy 使用）
VITE_API_BASE_URL=/api
# 按服务前缀强制走真实后端（逗号分隔），用于后端逐服务灰度切换，如：auth,tenant
VITE_REMOTE_SERVICES=
# 平台演示总入口地址（登录页「返回入口」跳转用）
VITE_PORTAL_URL=http://localhost:5172
# ===== Deepseek AI 出题（可选） =====
# 将 Key 写入 .env.local（已被 .gitignore 排除，禁止提交）即可启用真实 AI 生成；
# 未配置时 AI 出题自动回退本地演示数据。开发环境经 vite proxy /deepseek 转发。
# VITE_DEEPSEEK_API_KEY=sk-xxxxxxxx
# VITE_DEEPSEEK_BASE_URL=/deepseek
# VITE_DEEPSEEK_MODEL=deepseek-chat
# ===== AI 拍照识题（可选） =====
# 视觉通道默认复用上方 Deepseek 的地址与 Key —— 即「请求 Deepseek 分析图片」，
# 只需单独指定模型名。注意 Deepseek 官方模型不支持图片输入：走官方接口时需由
# 支持视觉的网关/代理转发，或用下面两项改指任意 OpenAI 兼容多模态端点
# （如 DashScope 兼容模式 target=https://dashscope.aliyuncs.com prefix=/compatible-mode/v1）。
# VITE_VISION_MODEL=qwen-vl-max
# VITE_VISION_BASE_URL=/vision
# VITE_VISION_API_KEY=sk-xxxxxxxx

